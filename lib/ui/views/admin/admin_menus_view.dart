import 'package:flutter/material.dart';

class AdminMenusView extends StatelessWidget {
  const AdminMenusView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Revisión de Menús",
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600),
      ),
    );
  }
}
